{
  "name": "xero-report-downloader-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local proxy backend for the Xero Report Downloader: handles OAuth 2.0 and relays Xero API calls past CORS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "smoke": "node smoke-test.mjs",
    "render-test": "node render-test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jspdf": "^2.5.1",
    "jspdf-autotable": "^3.8.2",
    "jszip": "^3.10.1",
    "xlsx": "^0.18.5"
  }
}
